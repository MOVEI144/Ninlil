# Field-network design history

The original September 8 proposal is preserved byte-for-byte in Git.
Its source revision, path and SHA-256 are in [the verified index](HISTORICAL_FIELD_DESIGN.json).
Retrieve it with git show REVISION:PATH using those fields.
Current behavior and limitations are in [the deployment record](FIELD_DEPLOYMENT_2026-09-08.md).
The September 9 user decision requires product-independent operation, Root replacement
without touching children, battery sleep, deployment transfer and address reuse.
