#ifndef NINLIL_NATIVE_SDKCONFIG_H
#define NINLIL_NATIVE_SDKCONFIG_H
/* Native ESP boundary tests use command-line CONFIG_* definitions. Actual
 * ESP-IDF builds use the generated sdkconfig.h and never this directory. */
#endif
