--- third_party/libedhoc/backends/cbor/src/backend_cbor_sig_structure_encode.c
+++ third_party/adapted/cbor/backend_cbor_sig_structure_encode.c
@@ -18,6 +18,12 @@
 
 static bool encode_sig_structure(zcbor_state_t *state, const struct sig_structure *input);
 
+
+/* Exact callback types: no incompatible function-pointer casts. */
+static bool ninlil_decoder_encode_sig_structure(zcbor_state_t *state, void *data)
+{
+    return encode_sig_structure(state, data);
+}
 
 static bool encode_sig_structure(
 		zcbor_state_t *state, const struct sig_structure *input)
@@ -50,5 +56,5 @@
 	zcbor_state_t states[3];
 
 	return zcbor_entry_function(payload, payload_len, (void *)input, payload_len_out, states,
-		(zcbor_decoder_t *)encode_sig_structure, sizeof(states) / sizeof(zcbor_state_t), 1);
+		ninlil_decoder_encode_sig_structure, sizeof(states) / sizeof(zcbor_state_t), 1);
 }
