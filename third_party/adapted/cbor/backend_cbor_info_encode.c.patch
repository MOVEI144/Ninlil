--- third_party/libedhoc/backends/cbor/src/backend_cbor_info_encode.c
+++ third_party/adapted/cbor/backend_cbor_info_encode.c
@@ -18,6 +18,12 @@
 
 static bool encode_info(zcbor_state_t *state, const struct info *input);
 
+
+/* Exact callback types: no incompatible function-pointer casts. */
+static bool ninlil_decoder_encode_info(zcbor_state_t *state, void *data)
+{
+    return encode_info(state, data);
+}
 
 static bool encode_info(
 		zcbor_state_t *state, const struct info *input)
@@ -48,5 +54,5 @@
 	zcbor_state_t states[2];
 
 	return zcbor_entry_function(payload, payload_len, (void *)input, payload_len_out, states,
-		(zcbor_decoder_t *)encode_info, sizeof(states) / sizeof(zcbor_state_t), 3);
+		ninlil_decoder_encode_info, sizeof(states) / sizeof(zcbor_state_t), 3);
 }
