--- third_party/libedhoc/backends/cbor/src/backend_cbor_plaintext_3_encode.c
+++ third_party/adapted/cbor/backend_cbor_plaintext_3_encode.c
@@ -27,6 +27,22 @@
 static bool encode_plaintext_3(zcbor_state_t *state, const struct plaintext_3 *input);
 
 
+/* Exact callback types: no incompatible function-pointer casts. */
+static bool ninlil_decoder_encode_plaintext_3(zcbor_state_t *state, void *data)
+{
+    return encode_plaintext_3(state, data);
+}
+
+static bool ninlil_encoder_encode_ead_y(zcbor_state_t *state, const void *data)
+{
+    return encode_ead_y(state, data);
+}
+
+static bool ninlil_encoder_zcbor_bstr_encode(zcbor_state_t *state, const void *data)
+{
+    return zcbor_bstr_encode(state, data);
+}
+
 static bool encode_repeated_map_kid(
 		zcbor_state_t *state, const struct map_kid_r *input)
 {
@@ -53,7 +69,7 @@
 	zcbor_log("%s\r\n", __func__);
 
 	bool tmp_result = (((((*input).COSE_X509_choice == COSE_X509_bstr_c) ? ((zcbor_bstr_encode(state, (&(*input).COSE_X509_bstr))))
-	: (((*input).COSE_X509_choice == COSE_X509_certs_l_c) ? ((zcbor_list_start_encode(state, 3) && ((zcbor_multi_encode_minmax(2, 3, &(*input).COSE_X509_certs_l_certs_count, (zcbor_encoder_t *)zcbor_bstr_encode, state, (&(*input).COSE_X509_certs_l_certs), sizeof(struct zcbor_string))) || (zcbor_list_map_end_force_encode(state), false)) && zcbor_list_end_encode(state, 3)))
+	: (((*input).COSE_X509_choice == COSE_X509_certs_l_c) ? ((zcbor_list_start_encode(state, 3) && ((zcbor_multi_encode_minmax(2, 3, &(*input).COSE_X509_certs_l_certs_count, ninlil_encoder_zcbor_bstr_encode, state, (&(*input).COSE_X509_certs_l_certs), sizeof(struct zcbor_string))) || (zcbor_list_map_end_force_encode(state), false)) && zcbor_list_end_encode(state, 3)))
 	: false))));
 
 	if (!tmp_result) {
@@ -164,7 +180,7 @@
 {
 	zcbor_log("%s\r\n", __func__);
 
-	bool tmp_result = (zcbor_multi_encode_minmax(1, 3, &(*input).EAD_3_count, (zcbor_encoder_t *)encode_ead_y, state, (&(*input).EAD_3), sizeof(struct ead_y)));
+	bool tmp_result = (zcbor_multi_encode_minmax(1, 3, &(*input).EAD_3_count, ninlil_encoder_encode_ead_y, state, (&(*input).EAD_3), sizeof(struct ead_y)));
 
 	if (!tmp_result) {
 		zcbor_trace_file(state);
@@ -208,5 +224,5 @@
 	zcbor_state_t states[6];
 
 	return zcbor_entry_function(payload, payload_len, (void *)input, payload_len_out, states,
-		(zcbor_decoder_t *)encode_plaintext_3, sizeof(states) / sizeof(zcbor_state_t), 8);
-}
+		ninlil_decoder_encode_plaintext_3, sizeof(states) / sizeof(zcbor_state_t), 8);
+}
