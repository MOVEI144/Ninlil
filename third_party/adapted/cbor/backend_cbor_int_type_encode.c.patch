--- third_party/libedhoc/backends/cbor/src/backend_cbor_int_type_encode.c
+++ third_party/adapted/cbor/backend_cbor_int_type_encode.c
@@ -18,6 +18,12 @@
 
 static bool encode_integer_type_int_type(zcbor_state_t *state, const int32_t *input);
 
+
+/* Exact callback types: no incompatible function-pointer casts. */
+static bool ninlil_decoder_encode_integer_type_int_type(zcbor_state_t *state, void *data)
+{
+    return encode_integer_type_int_type(state, data);
+}
 
 static bool encode_integer_type_int_type(
 		zcbor_state_t *state, const int32_t *input)
@@ -46,5 +52,5 @@
 	zcbor_state_t states[2];
 
 	return zcbor_entry_function(payload, payload_len, (void *)input, payload_len_out, states,
-		(zcbor_decoder_t *)encode_integer_type_int_type, sizeof(states) / sizeof(zcbor_state_t), 1);
+		ninlil_decoder_encode_integer_type_int_type, sizeof(states) / sizeof(zcbor_state_t), 1);
 }
